# Handoff: Blender 中文輸入法重複字元修復專案 (Pure Python + Win32 API)

**最後更新時間**: 2026-08-16 23:30  
**當前穩定版本**: **v20（攔截名單版 — 程式碼維持原樣不動）**  
**核心檔案**: [ime_fix_poc.py](file:///c:/Users/Senpai/OneDrive/Work/GitHub/Blender_IME_Input_Fix/ime_fix_poc.py)  
**詳細歷程文件**: [handoffs/2026-08-16-blender-ime-fix-v20.md](file:///c:/Users/Senpai/OneDrive/Work/GitHub/Blender_IME_Input_Fix/handoffs/2026-08-16-blender-ime-fix-v20.md)

---

## 1. 專案目標與背景
* **問題描述**：在 Windows 平台使用 Blender (2.8+ / 4.x / 5.2) 時，若切換為微軟中文輸入法（注音 / 拼音），輸入數字（0-9）或特定 Shift 符號（@, #, %, &, *, +, {, }, | 等）時，文字框會出現**重複字元**。
* **核心約束與目標**：
  * **純 Python + ctypes 實作**：完全不依賴外部編譯的 C/C++ DLL，達成零編譯依賴、跨 Blender 版本通用。
  * 支援常規文字輸入框（屬性面板、Outliner 物件重命名、3D 文字、VSE 等）。
  * 保持中文字詞組合、選字視窗、Enter 提交與英文模式完全正常。

---

## 2. v20 測試結果最新回報 (2026-08-16 23:29)

### 總體進度：
**中文、英文、數字輸入大部分皆已正常！**

### 詳細矩陣：
1. **中文輸入**：✅ 正常（選字 + Enter 正常保留）
2. **Shift 符號 (`@ # % & *`)**：✅ 正常（單次輸出）
3. **基本符號與英文字母 (`! $ 英文字母`)**：✅ 正常（單次輸出）
4. **數字鍵 (頂部與九宮格 `0-9`)**：✅ 正常（單次輸出）
5. **英文輸入法模式**：✅ 正常
6. **大括號 (`{}`)**：✅ 正常
7. **等號鍵直接輸入 (`=`)**：❌ **重複變成 `==`**
8. **按住 Shift 輸入等號鍵 (`+`)**：❌ **重複出現 `++`**（正常應為單個 `+`）

---

## 3. 當前策略與待辦

* **程式碼保持原樣**：`ime_fix_poc.py` 目前嚴格保持在 v20，不做任何改動。
* **待分析問題**：
  1. `=` 重複（未在 v20 攔截名單中）。
  2. Shift+`=` 出現 `++` 的精確訊息流分析。
